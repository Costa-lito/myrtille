% ============================================================
%  Myrtille — Charte Graphique — Style moderne
% ============================================================
\documentclass[a4paper,10pt]{article}

% ── Encodage ─────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ── Police sans-serif globale (Helvetica / Courier) ──────────
\usepackage[scaled=0.92]{helvet}
\usepackage{courier}
\renewcommand{\familydefault}{\sfdefault}

% ── Mise en page ─────────────────────────────────────────────
\usepackage[a4paper,
            top=20mm, bottom=22mm,
            left=20mm, right=20mm,
            headheight=16pt]{geometry}
\usepackage{parskip}          % espacement entre paragraphes, pas d'indentation
\usepackage{setspace}
\setstretch{1.08}

% ── Couleurs ─────────────────────────────────────────────────
\usepackage{xcolor}
\definecolor{accent}    {HTML}{0D6EFD}   % bleu vif principal
\definecolor{accentDark}{HTML}{0A4FA8}   % bleu foncé
\definecolor{accentPale}{HTML}{E8F1FF}   % bleu très clair fond
\definecolor{dark}      {HTML}{1A1D23}   % quasi-noir texte
\definecolor{coverBg}   {HTML}{0F2A4A}   % fond couverture bleu marine
\definecolor{mid}       {HTML}{4A5060}   % gris-bleu texte secondaire
\definecolor{light}     {HTML}{F0F2F5}   % fond clair
\definecolor{border}    {HTML}{D0D6E0}   % bords tableaux
\definecolor{codeBg}    {HTML}{F1F4F9}   % fond code light
\definecolor{codeText}  {HTML}{1A2540}   % texte code
\definecolor{codeKw}    {HTML}{0550AE}   % keywords
\definecolor{codeStr}   {HTML}{1A7F37}   % strings
\definecolor{codeCmt}   {HTML}{7A8599}   % commentaires
\definecolor{inlineCode}{HTML}{0D6EFD}   % code inline couleur
\definecolor{inlineBg}  {HTML}{EEF3FF}   % code inline fond
\definecolor{success}   {HTML}{198754}
\definecolor{warn}      {HTML}{FFC107}
\definecolor{warnDark}  {HTML}{664D03}
\definecolor{warnBg}    {HTML}{FFF8E1}
\definecolor{infoGreen} {HTML}{0F5132}
\definecolor{infoBg}    {HTML}{D1E7DD}
\definecolor{tableHdr}  {HTML}{E8F1FF}
\definecolor{sidebarBg} {HTML}{F6F8FC}

% Couleur personnalisée pour l'exemple de création de badge
\definecolor{customPurple}{HTML}{8E44AD} 

% ── TikZ (décorations page de garde + badges) ────────────────
\usepackage{tikz}
\usetikzlibrary{calc,positioning,shapes.geometric}

% Macro pour afficher les palettes de couleurs dans le document
\newcommand{\colorswatch}[2]{%
  \begin{tikzpicture}
    \fill[#1, rounded corners=3pt] (0,0) rectangle (2.2,1.2);
    \node[below, font=\sffamily\scriptsize, text=mid, align=center] at (1.1,0) {\textbf{#1}\\#2};
  \end{tikzpicture}\hspace{10pt}
}

% ── En-têtes / pieds ─────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{1.2pt}
\renewcommand{\headrule}{%
  \vspace{2pt}%                    % espace entre texte et liseré
  {\color{accent}\rule{\linewidth}{\headrulewidth}}%
}
\fancyhead[L]{\color{mid}\small\sffamily Myrtille \textbf{·} Charte Graphique}
\fancyhead[R]{\color{mid}\small\sffamily\leftmark}
\fancyfoot[C]{\color{mid}\small\thepage}

% ── Titres de sections ───────────────────────────────────────
\usepackage{titlesec}

% Section : Numéro dans badge avec ajustement optique vers le haut
\titleformat{\section}{}{}{0pt}{%
  \normalfont\sffamily\Large\bfseries\color{dark}%
  \begin{tikzpicture}[baseline=0pt] 
    \node[
      fill=accent, 
      text=white, 
      rounded corners=3pt,
      inner xsep=9pt,    
      inner ysep=4pt,    
      font=\normalsize\bfseries,
      anchor=base,       
      yshift=1.5pt        
    ] {\thesection};
  \end{tikzpicture}\hspace{10pt}%
}
\titleformat{name=\section,numberless}{}{}{0pt}{%
  \normalfont\sffamily\Large\bfseries\color{dark}%
}
\titlespacing*{\section}{0pt}{22pt}{8pt}

\titleformat{\subsection}{\sffamily\large\bfseries\color{accentDark}}{%
  \color{accentDark}\thesubsection}{8pt}{}
\titlespacing*{\subsection}{0pt}{14pt}{4pt}

\titleformat{\subsubsection}{\sffamily\normalsize\bfseries\color{mid}}{%
  \color{mid}\thesubsubsection}{8pt}{}
\titlespacing*{\subsubsection}{0pt}{10pt}{2pt}

% ── Hyperref ─────────────────────────────────────────────────
\usepackage{hyperref}
\hypersetup{
  colorlinks = true,
  linkcolor  = accent,
  urlcolor   = accent,
  citecolor  = accent,
  pdftitle   = {Myrtille — Charte Graphique},
}

% ── Table des matières ───────────────────────────────────────
\usepackage{tocloft}
\renewcommand{\cftsecfont}{\sffamily\bfseries\color{dark}}
\renewcommand{\cftsecpagefont}{\sffamily\bfseries\color{accent}}
\renewcommand{\cftsecdotsep}{\cftnodots}
\renewcommand{\cftsubsecfont}{\sffamily\color{mid}}
\renewcommand{\cftsubsecpagefont}{\sffamily\color{mid}}
\renewcommand{\cftsubsubsecfont}{\sffamily\small\color{mid}}
\setlength{\cftbeforesecskip}{6pt}
\setlength{\cftbeforesubsecskip}{2pt}
\renewcommand{\contentsname}{}   

% ── Listes ───────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{
  leftmargin  = 1.4em,
  itemsep     = 3pt,
  topsep      = 4pt,
  label       = \textcolor{accent}{\textbullet},
}
\setlist[enumerate]{
  leftmargin  = 1.6em,
  itemsep     = 3pt,
  topsep      = 4pt,
  label       = \textcolor{accent}{\arabic*.},
}
\setlist[itemize,2]{label=\textcolor{mid}{--}}

% ── Tableaux ─────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{colortbl}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\renewcommand{\arraystretch}{1.4}
\setlength{\arrayrulewidth}{0.4pt}

% ── Blocs de code ────────────────────────────────────────────
\usepackage{listings}
\lstdefinestyle{pycode}{
  language          = Python,
  backgroundcolor   = \color{codeBg},
  basicstyle        = \ttfamily\footnotesize\color{codeText},
  keywordstyle      = \color{codeKw}\bfseries,
  stringstyle       = \color{codeStr},
  commentstyle      = \color{codeCmt}\itshape,
  numberstyle       = \tiny\color{codeCmt},
  numbers           = left,
  numbersep         = 10pt,
  breaklines        = true,
  showstringspaces  = false,
  frame             = none,
  xleftmargin       = 16pt,
  xrightmargin      = 6pt,
  aboveskip         = 0pt,
  belowskip         = 0pt,
  literate          =
    {é}{{\'e}}1 {è}{{\`e}}1 {ê}{{\^e}}1 {à}{{\`a}}1
    {â}{{\^a}}1 {ç}{{\c{c}}}1 {µ}{{$\mu$}}1,
}
\lstdefinestyle{jsoncode}{
  language          = {},
  backgroundcolor   = \color{codeBg},
  basicstyle        = \ttfamily\footnotesize\color{codeText},
  stringstyle       = \color{codeStr},
  numbers           = left,
  numberstyle       = \tiny\color{codeCmt},
  numbersep         = 10pt,
  breaklines        = true,
  frame             = none,
  xleftmargin       = 16pt,
  xrightmargin      = 6pt,
  aboveskip         = 0pt,
  belowskip         = 0pt,
  morestring        = [b]",
}
\lstdefinestyle{shcode}{
  language          = {},
  backgroundcolor   = \color{codeBg},
  basicstyle        = \ttfamily\footnotesize\color{codeText},
  numbers           = none,
  breaklines        = true,
  frame             = none,
  xleftmargin       = 12pt,
  aboveskip         = 0pt,
  belowskip         = 0pt,
}
\lstset{style=pycode}

% ── Environnement code complet (avec arrondi) ─────────────────
\usepackage{tcolorbox}
\tcbuselibrary{breakable, skins, listings}

\newtcblisting{pyblock}{
  breakable,
  listing only,
  listing style    = pycode,
  colback          = codeBg,
  colframe         = accent,
  arc              = 4pt,
  leftrule         = 3pt, rightrule = 0.4pt, toprule = 0.4pt, bottomrule = 0.4pt,
  top              = 6pt, bottom = 6pt, left = 0pt, right = 4pt,
  before skip      = 8pt, after skip = 8pt,
}

\newtcblisting{jsonblock}{
  breakable,
  listing only,
  listing style    = jsoncode,
  colback          = codeBg,
  colframe         = mid,
  arc              = 4pt,
  leftrule         = 3pt, rightrule = 0.4pt, toprule = 0.4pt, bottomrule = 0.4pt,
  top              = 6pt, bottom = 6pt, left = 0pt, right = 4pt,
  before skip      = 8pt, after skip = 8pt,
}

\newtcblisting{shblock}{
  breakable,
  listing only,
  listing style    = shcode,
  colback          = codeBg,
  colframe         = codeCmt,
  arc              = 4pt,
  leftrule         = 3pt, rightrule = 0.4pt, toprule = 0.4pt, bottomrule = 0.4pt,
  top              = 6pt, bottom = 6pt, left = 0pt, right = 4pt,
  before skip      = 8pt, after skip = 8pt,
}

% ── Boîtes Info / Attention ───────────────────────────────────
\newtcolorbox{infobox}[1][]{
  breakable,
  colback   = accentPale,
  colframe  = accent,
  arc       = 3pt,
  leftrule  = 4pt, rightrule = 0.4pt, toprule = 0.4pt, bottomrule = 0.4pt,
  left      = 8pt, right = 8pt, top = 6pt, bottom = 6pt,
  before skip = 8pt, after skip = 8pt,
  fontupper = \sffamily\small\color{accentDark},
  #1
}

\newtcolorbox{warnbox}[1][]{
  breakable,
  colback   = warnBg,
  colframe  = warn,
  arc       = 3pt,
  leftrule  = 4pt, rightrule = 0.4pt, toprule = 0.4pt, bottomrule = 0.4pt,
  left      = 8pt, right = 8pt, top = 6pt, bottom = 6pt,
  before skip = 8pt, after skip = 8pt,
  fontupper = \sffamily\small\color{warnDark},
  #1
}

% ── Badge / Tag inline ────────────────────────────────────────
\newcommand{\ci}[1]{%
  \texttt{\small\color{inlineCode}#1}%
}

\newcommand{\badge}[2][accent]{%
  \tikz[baseline=-2.5pt]{%
    \node[fill=#1!15, draw=#1!40, text=#1, rounded corners=2pt,
          inner xsep=4pt, inner ysep=1.5pt,
          font=\ttfamily\tiny\bfseries] {#2};%
  }%
}
\newcommand{\badgeReq}{\badge[success]{Requis}}
\newcommand{\badgeOpt}{\badge[mid]{Optionnel}}
\newcommand{\badgeInt}{\badge[accent]{int}}
\newcommand{\badgeFloat}{\badge[accent]{float}}
\newcommand{\badgeStr}{\badge[accent]{str}}
\newcommand{\badgeMat}{\badge[accentDark]{mat}}
\newcommand{\badgePolar}{\badge[accentDark]{polar}}

% Exemples de badges personnalisés
\newcommand{\badgeNouveau}{\badge[infoGreen]{Nouveau}}
\newcommand{\badgePerso}{\badge[customPurple]{Spécial}}

% ── Ligne de séparation ───────────────────────────────────────
\newcommand{\hrulefull}{%
  \vspace{2pt}\noindent\textcolor{border}{\rule{\linewidth}{0.5pt}}\vspace{6pt}%
}

% ── Note de marge latérale simulée ───────────────────────────
\newcommand{\sidenote}[1]{%
  \begin{tcolorbox}[
    colback=sidebarBg, colframe=border, arc=3pt,
    left=8pt, right=8pt, top=5pt, bottom=5pt,
    before skip=6pt, after skip=6pt,
    fontupper=\sffamily\footnotesize\color{mid}
  ]#1\end{tcolorbox}%
}

% ============================================================
\begin{document}
% ============================================================

\begin{titlepage}
\thispagestyle{empty}
\begin{tikzpicture}[remember picture, overlay]
  % 1. Insertion du fond PDF (ancré en bas à gauche pour couvrir toute la page)
  \node[anchor=south west, inner sep=0pt, outer sep=0pt] at (current page.south west) {
    \includegraphics[width=\paperwidth,height=\paperheight]{cover_bg.pdf}
  };

  % 2. Badge de version conservé en LaTeX (dynamique)
  \node[fill=accent!20, draw=accent!50, text=accent!80,
        rounded corners=3pt, font=\sffamily\small\bfseries,
        inner xsep=8pt, inner ysep=4pt]
    at ($(current page.north east)+(-3cm,-1.8cm)$) {v1.0};
\end{tikzpicture}

\vspace*{4.5cm}
\hspace*{1cm}%
\begin{minipage}{0.85\textwidth}
  {\color{accent}\sffamily\footnotesize\bfseries\MakeUppercase{Documentation}}

  \vspace{0.3cm}
  {\color{white}\fontsize{46}{52}\selectfont\sffamily\bfseries Myrtille}

  \vspace{0.5cm}
  {\color{white!70}\large\sffamily Charte Graphique \& guide d'utilisation}

  \vspace{1.4cm}
  \begin{tabular}{@{}ll@{}}
    \color{white!50}\sffamily\small Identité visuelle &
      \color{accent}\sffamily\small \textbullet\ Palette de couleurs, typographie \\[3pt]
    \color{white!50}\sffamily\small Composants &
      \color{accent}\sffamily\small \textbullet\ Badges, boîtes d'information, notes \\[3pt]
    \color{white!50}\sffamily\small Blocs de code &
      \color{accent}\sffamily\small \textbullet\ Python, JSON, Shell \\[3pt]
    \color{white!50}\sffamily\small Éléments de structure &
      \color{accent}\sffamily\small \textbullet\ Listes, tableaux, séparateurs \\
  \end{tabular}

  \vspace{0.5cm}
  {\color{white!35}\sffamily\footnotesize
    Guide de référence pour la rédaction documentaire}
\end{minipage}
\end{titlepage}

\setcounter{page}{1}

% ── TABLE DES MATIÈRES ───────────────────────────────────────
{\sffamily\Large\bfseries\color{dark} Table des matières}
\vspace{4pt}\hrulefull
\tableofcontents

\newpage

\include{content}

\end{document}